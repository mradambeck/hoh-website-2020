# ConvertKit (Kit) — Settings > Advanced > API Key, and the numeric ID of the
# form you want signups to land in. Both are safe to expose client-side;
# this is the public key, not the account API secret.
VITE_CONVERTKIT_FORM_ID=
VITE_CONVERTKIT_API_KEY=
