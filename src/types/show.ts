export interface Show {
  /** ISO date string, e.g. "2026-09-12" */
  date: string;
  venue: string;
  city: string;
  ticketUrl?: string;
  /** ISO date string, e.g. "2026-09-12" */
  announceDate?: string;
}
