name = "hoh-decap-oauth"
main = "src/index.js"
compatibility_date = "2024-11-01"

# GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET are secrets, not vars — set them
# with `wrangler secret put`, don't put them in this file. See README.
